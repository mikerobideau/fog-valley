import {CharacterTranscripts} from "../model/Characters";

export const Allen: CharacterTranscripts = {
    name: 'Allen',
    transcripts: [{
        title: "That's How They Get Ya",
        secret: 'fingerprints'
    }]
}